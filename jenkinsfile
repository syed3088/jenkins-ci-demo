pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo "Building project..."
                sh 'ls -la'
            }
        }

        stage('Test') {
            steps {
                echo "Testing project..."
            }
        }

    }
}
